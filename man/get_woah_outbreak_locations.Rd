% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hello.R
\name{get_woah_outbreak_locations}
\alias{get_woah_outbreak_locations}
\title{Fetch Outbreak Location Data from WOAH API}
\usage{
get_woah_outbreak_locations(
  start_date,
  end_date,
  disease_name = NULL,
  language = "en",
  verbose = FALSE
)
}
\arguments{
\item{start_date}{Character string or Date object for the start of the event date range (YYYY-MM-DD).}

\item{end_date}{Character string or Date object for the end of the event date range (YYYY-MM-DD).}

\item{disease_name}{Optional: Character string specifying the exact disease name.
If NULL or empty, fetches data for all diseases. Uses internal `get_woah_disease_id`.}

\item{language}{Language code (default: "en").}

\item{verbose}{Logical: Print progress messages? (Default: FALSE)}
}
\value{
A data frame containing the outbreak location data (longitude, latitude, etc.),
  an empty data frame if no records are found, or NULL if an error occurs.
}
\description{
Retrieves outbreak geographic location data (lat/lon) from the WOAH API,
allowing filtering by disease name and date range.
}
\examples{
\dontrun{
  # Get locations for Foot and Mouth disease outbreaks in March 2025
  fmd_locations <- get_woah_outbreak_locations(
    start_date = "2025-03-01",
    end_date = "2025-03-26",
    disease_name = "Foot and mouth disease virus (Inf. with) "
  )
  if (!is.null(fmd_locations)) print(utils::head(fmd_locations))

  # Get locations for all outbreaks in the first week of March 2025
  all_locations <- get_woah_outbreak_locations(
    start_date = "2025-03-01",
    end_date = "2025-03-07",
    disease_name = NULL # Or omit the parameter
  )
  if (!is.null(all_locations)) print(utils::head(all_locations))
}
}
